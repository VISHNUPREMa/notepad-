<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Content</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #EEF5FF;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .container-custom {
       
      background-color: white;
      padding: 40px; /* Increased padding */
      border-radius: 30px;
      margin-top: 50px;
      border: 2px solid #332941;
      width: 80%; /* Adjusted width */
      max-width: 1000px; /* Set a maximum width */
      box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1); /* Added shadow */
    }

    .title-input,
    .content-input {
      text-align: center;
    }

    .btn-library {
      display: flex;
      justify-content: space-between;
      margin-top: 20px; /* Increased top margin */
    }

    .btn-primary {
      background-color: #332941;
      border-color: #332941;
    }

    .form-control {
      border-color: #332941;
    }

    textarea.form-control {
      height: 250px; /* Increased height of textarea */
    }
  </style>
</head>
<body>
  <div class="container text-center">
    <div class="container-custom">
      <form action="/notes/library/edit/<%= result._id %>" method="POST">
        <h1 class="mb-4">Edit Content</h1>
        <div class="row mb-4 title-input"> <!-- Increased margin bottom -->
          <div class="col-md-12">
            <input type="text" class="form-control" id="titleInput" name="title" value="<%= result.title %>" required>
          </div>
        </div>
        <div class="row mb-4 content-input"> <!-- Increased margin bottom -->
          <div class="col-md-12">
            <textarea class="form-control" id="contentInput" rows="8" name="content" required><%= result.content %></textarea>
          </div>
        </div>
        <div class="row btn-library">
          <div class="col-md-12">
            <button type="submit" class="btn btn-primary me-2">Update</button>
            <a href="/notes/library" class="btn btn-primary">Go To Library</a>
          </div>
        </div>
      </form>
    </div>
  </div>
</body>
</html>



