<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Notepad Example</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #EEF5FF;
      margin: 0;
      padding: 0;
      
    }
  
    .container-custom {
      background-color: white;
      padding: 30px;
      border-radius: 30px;
      margin-top: 50px;
      border: 2px solid #332941;
    }
    
    .title-input,
    .content-input {
      text-align: center;
    }

    .btn-library {
      display: flex;
      justify-content: space-between;
    }

    .btn-primary {
      background-color: #332941;
      border-color: #332941;
    }

    .form-control {
      border-color: #332941;
    }

    textarea.form-control {
      height: 200px; /* Increase the height of the textarea */
    }
  </style>
</head>
<body>
  <div class="container-fluid p-0">
    <div class="container text-center">
      <div class="container-custom">
        <form action="/notes/add" method="post">
          <h1 class="mb-4">Notepad</h1>
          
          <div class="row mb-3 title-input">
            <div class="col-md-12">
              <input type="text" class="form-control" id="titleInput" placeholder="Enter the title name" name="title" required>
            </div>
          </div>
          
          <div class="row mb-3 content-input">
            <div class="col-md-12">
              <textarea class="form-control" id="contentInput" rows="8" placeholder="Enter the content" name="content" required></textarea>
            </div>
          </div>
          
          <div class="row btn-library">
            <div class="col-md-12">
              <button type="submit" class="btn btn-primary me-2">Submit</button>
              <a href="/notes/library" class="btn btn-primary">Go To Library</a>
            </div>
          </div>
          
          <% if(locals.message) { %>
            <div class="alert alert-danger mt-4"> <%= locals.message %></div>
          <% } %>

          <div class="container mt-4">
            <div class="row">
              <div class="col-md-12">
                <h2><%= demotitle%></h2>
                <h5><%= democontent%></h5>
              </div>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>





